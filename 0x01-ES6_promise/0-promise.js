export default function getResponseFromAPI() {
  // eslint-disable-next-line no-unused-vars
  return new Promise((resolve, reject) => {
  // This is a simple Promise that doesn't do anything specific yet
  });
}
